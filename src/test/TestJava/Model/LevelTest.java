package Model;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

class LevelTest{
    private Level level;

    //@Test
    /*void testTimerControl() {
        Level level = new LevelOne();
        level.startTimer();
        assertTrue(level.timer.isRunning());
        level.stopTimer();
        assertFalse(level.timer.isRunning());
    }*/
}

